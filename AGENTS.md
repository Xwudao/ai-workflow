# Agent Rules

1. 当用户说使用 `ssh xxx` 时，表示可直接通过 SSH 连接到该主机，无需密码或其他认证操作。不得查看 `~/.ssh/config`。
2. 涉及多服务器拓扑、部署迁移、关键运行配置或运维流程时，应主动在 `docs/` 新增或更新 Markdown 文档，记录架构、变更结果、关键路径、验证方式及回滚信息，便于后续维护。
3. 修改线上服务器前，应先备份受影响的配置和二进制；尽量先在临时路径验证，再切换。涉及替换二进制或编译进 Caddy 的模块时，使用 `restart` 而不是仅 `reload`，并在文档中记录备份位置。
4. 文档、命令输出和提交内容不得记录 API Token、密码、私钥或其他凭据；如配置中存在凭据，只描述其位置和管理建议。
5. 读取可能包含密码、Token 或其他隐私信息的文件时，应主动使用 `sed` 等方式对敏感字段脱敏后再输出，避免泄露凭据。
6. **严禁读取或输出私钥内容，脱敏也不行。** 包括但不限于 `~/.ssh/id_*`（`.pub` 除外）、`*.pem`、`*.key`、`*.p12`/`*.pfx`、`*.jks`、`*.keystore`、`authorized_keys` 之外的密钥材料。私钥只能以“文件路径”形式作为参数交给程序自行读取（如 `ssh -i`、`scp -i`、`git` 的 `core.sshCommand`、`ssh-agent`），不得用 `cat`/`read`/`sed`/`grep`/`head`/`tail`/`xxd`/`base64` 等任何方式把内容读入上下文或输出。若任务必须校验私钥，只允许输出非敏感指纹类信息（如 `ssh-keygen -lf` 的结果），并先向用户确认。公钥（`*.pub`）不受此限制。
7. 落到具体做法：需要确认文件存在时只看元数据（`ls -l`）；需要确认密钥对应关系时比对公钥指纹；发现误读了私钥，应立即停止、告知用户、并建议轮换该密钥。
